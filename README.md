# Nero AI Bot 🤖

**Nero AI Bot** — это продвинутый Telegram-бот, готовый к работе в продакшене (production-ready). Он использует современные LLM-модели (Large Language Models) для выполнения широкого спектра задач: от генерации текста и изображений до анализа документов (RAG) и обработки голосовых сообщений.

## ✨ Ключевые Возможности

*   **Мультимодельный Чат**: Основная модель — Google **Gemini** (через Artemox API) с автоматическим переключением (fallback) на **DeepSeek** и **OpenAI** в случае недоступности.
*   **Генерация Изображений**: Создание качественных изображений с помощью моделей **Imagen 3/4** в различных стилях (Аниме, Реализм, 3D и др.).
*   **RAG (Retrieval Augmented Generation)**: Загрузка PDF-документов и возможность задавать вопросы по их содержанию. Используется векторная база данных **ChromaDB**.
*   **Голосовые Сообщения**: Поддержка голосового ввода (Speech-to-Text) и озвучивания ответов (Text-to-Speech).
*   **Система Персонажей**: Переключение между различными личностями ИИ (Программист, Учитель, Психолог и др.).
*   **Админ-панель**: Встроенный веб-интерфейс на **Streamlit** для управления пользователями, рассылками и просмотра аналитики.
*   **Монетизация**: Интеграция с **Telegram Stars** для подписок и платных функций.
*   **Геймификация**: Система достижений и статистика использования.
*   **Надежная Архитектура**:
    *   Полностью асинхронный код (AsyncIO).
    *   Паттерн **Circuit Breaker** для стабильной работы с API.
    *   Контейнеризация **Docker** для простого развертывания.
    *   Поддержка **PostgreSQL/SQLite** через SQLAlchemy 2.0.
    *   **Redis** для очередей задач и кэширования.

## 🛠 Стек Технологий

*   **Язык**: Python 3.10+
*   **Фреймворк**: `python-telegram-bot` (v22+)
*   **База данных**: SQLAlchemy (Async), Alembic (миграции), SQLite/PostgreSQL
*   **Очереди задач**: Redis, Taskiq
*   **Observability**: Prometheus, Structlog
*   **Админка**: Streamlit

## 🚀 Начало Работы

### Требования

*   Docker & Docker Compose
*   Токен Telegram-бота (от [@BotFather](https://t.me/BotFather))
*   API-ключи (Artemox/Gemini, опционально OpenAI/DeepSeek)

### Установка (Docker)

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/your-username/nero-ai-bot.git
    cd nero-ai-bot
    ```

2.  **Настройте окружение:**
    Скопируйте пример файла конфигурации `.env.example` в `.env` и заполните необходимые значения.
    ```bash
    cp .env.example .env
    nano .env
    ```

    **Обязательные переменные:**
    *   `TELEGRAM_BOT_TOKEN`: Ваш токен Telegram-бота.
    *   `ARTEMOX_API_KEY`: API-ключ для моделей Gemini.

3.  **Запустите проект через Docker Compose:**
    ```bash
    docker-compose up -d --build
    ```

    После запуска бот начнет работу. Админ-панель будет доступна по адресу `http://localhost:8501`.

### Локальная Разработка

1.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

2.  **Примените миграции базы данных:**
    ```bash
    alembic upgrade head
    ```

3.  **Запустите бота:**
    ```bash
    python main.py
    ```

## 📂 Структура Проекта

*   `handlers/`: Обработчики обновлений Telegram (команды, сообщения, callback-кнопки).
*   `services/`: Бизнес-логика (интеграция с ИИ, генерация изображений, RAG).
*   `database/`: Модели базы данных и логика подключения.
*   `middlewares/`: Middleware для логирования, обработки ошибок и т.д.
*   `alembic/`: Скрипты миграции базы данных.
*   `utils/`: Вспомогательные функции и утилиты.
*   `locales/`: Файлы локализации.

## ⚙️ Конфигурация

Приложение настраивается через переменные окружения (проверяются в `config.py`).

| Переменная | Описание | Значение по умолчанию |
| :--- | :--- | :--- |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram-бота | **Обязательно** |
| `ARTEMOX_API_KEY` | API-ключ Gemini | **Обязательно** |
| `ADMIN_IDS` | ID администраторов через запятую | `""` |
| `OPENAI_API_KEY` | API-ключ OpenAI (Fallback) | `""` |
| `DEEPSEEK_API_KEY` | API-ключ DeepSeek (Fallback) | `""` |
| `USE_WEBHOOKS` | Использовать Webhooks вместо Polling | `False` |

## 🤝 Вклад в проект (Contributing)

Мы приветствуем вклад в развитие проекта! Пожалуйста, отправляйте Pull Request.

## 📄 Лицензия

[MIT License](LICENSE)
